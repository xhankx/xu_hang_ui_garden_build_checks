export interface TextProps {
    children: React.ReactNode;
    fontSize?: string;
    color?: string;
    fontWeight?: 'normal' | 'bold' | 'bolder' | 'lighter';
    lineHeight?: string;
    margin?: string;
  }
  